machine:
  # https://github.com/circleci/docker/blob/docker-1.9.1/circle.yml#L2-L5
  pre:
    - echo 'DOCKER_OPTS="-s btrfs -e lxc -D --userland-proxy=false"' | sudo tee -a /etc/default/docker
    - sudo curl -L -o /usr/bin/docker 'https://s3-external-1.amazonaws.com/circle-downloads/docker-1.9.1-circleci'
    - sudo chmod 0755 /usr/bin/docker
  services:
    - docker
  environment:
    SDK_VERSION: 10.11
    XCODE_VERSION: 7.1.1_7B1005

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - curl -L "${SDK_DOWNLOAD_URL}/MacOSX${SDK_VERSION}.sdk.Xcode${XCODE_VERSION}.tar.xz" > ./SDKs/MacOSX${SDK_VERSION}.sdk.Xcode${XCODE_VERSION}.tar.xz
    - docker version
    - docker info
    - if [[ -e ~/docker/ubuntu_wily.tar ]]; then docker load -i ~/docker/ubuntu_wily.tar; fi
    - docker build -t "osxcc/osxcc:latest" .
    - mkdir -p ~/docker; docker save ubuntu:wily > ~/docker/ubuntu_wily.tar

test:
  override:
    - docker run --rm -it "osxcc/osxcc:latest" env

deployment:
  prod:
    branch: master
    commands:
      - docker login -u $DOCKER_USER -p $DOCKER_PASSWORD -e $DOCKER_EMAIL
      - docker push "osxcc/osxcc:latest"
